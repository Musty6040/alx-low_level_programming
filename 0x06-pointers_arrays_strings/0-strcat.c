#include "main.h"

/**
 * _strcat - a function that links two strings together
 * @dest: copy to
 * @src: copy from
 * Return: two link  strings
 */
char *_strcat(char *dest, char *src)
{
	int j, n;

	for (j = 0; dest[j] != '\0'; j++)
	{
	}

	for (n = 0; (dest[j + n] = *src++) != '\0'; n++)
	{
	}

	return (dest);
}
